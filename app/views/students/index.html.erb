<h1>Studenten</h1>

<table>
  <tr>
    <th>First name</th>
    <th>Last name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @students.each do |student| %>
  <tr>
    <td><%= student.first_name %></td>
    <td><%= student.last_name %></td>
    <td><%= link_to 'Toon', student %></td>
    <td><%= link_to 'Pas aan', edit_student_path(student) %></td>
    <td><%= link_to 'Verwijder', student, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nieuwe Student', new_student_path %>
